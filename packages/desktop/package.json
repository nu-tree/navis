{
  "name": "navis-desktop",
  "version": "0.1.7",
  "private": true,
  "description": "navis 데스크톱 앱 (Electron — macOS/Windows)",
  "author": "nu-tree",
  "main": "electron-main.cjs",
  "scripts": {
    "web:build": "cd ../app && npx expo export -p web --output-dir ../desktop/web-build",
    "start": "electron .",
    "dev": "cross-env ELECTRON_DEV=1 electron .",
    "dist": "pnpm web:build && electron-builder",
    "dist:mac": "pnpm web:build && electron-builder --mac",
    "dist:win": "pnpm web:build && electron-builder --win",
    "release": "pnpm web:build && electron-builder --publish never && node scripts/upload.mjs"
  },
  "build": {
    "appId": "com.navis.app",
    "productName": "Navis",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "files": [
      "electron-main.cjs",
      "preload.cjs",
      "updater-config.json",
      "web-build/**/*"
    ],
    "publish": [
      {
        "provider": "generic",
        "url": "${env.EXPO_PUBLIC_NAVIS_URL}/api/desktop/file"
      }
    ],
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.productivity",
      "icon": "../app/assets/navis-logo.png"
    },
    "win": {
      "target": "nsis",
      "icon": "../app/assets/navis-logo.png"
    },
    "linux": {
      "target": "AppImage",
      "icon": "../app/assets/navis-logo.png"
    }
  },
  "dependencies": {
    "electron-updater": "^6.3.9",
    "serve-handler": "^6.1.6"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "33.4.11",
    "electron-builder": "^25.1.8"
  }
}
