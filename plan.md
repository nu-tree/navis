/\*\*

1. 모든 쓸모없는 코드를 지운다. -> 현재 병목 코드가 너무 많고 그래서인지 너무 느리다 필수 기능인 기억저장, 기억 불러오기를 제외한 부수적인 기능은 전부 지운다. (크론, 기억다이스트, 구글 캘린더, 커넥터, 알림등 전부 삭제)

2. 필수 기능만 목표로 웹으로 먼저 만든다 웹은 next로 진행할까 한다.

3. 다음으로 모노레포의 구성으로 확장성을 생각하며 만든다 웹을 만든 후에는 앱을 추가해서 스키마등 공용으로 쓸 수 있는건 쓴다

4. 폴더 구조는 이런느낌으로 간다
   packages/core ← 도메인 로직 (createOrder 등)
   packages/schema ← zod 스키마 + 타입
   apps/web ← 서버 액션이 core를 호출
   apps/mobile ← schema 타입으로 fetch

5. 현재는 배포를 생각하지말고 배포관련 코드는 전부 지운다 추 후 vercel이든 다른 서버를 생각한다 railway는 사용 안함

6. 빌드 스크립트나 이미지 만드는 스크립트나 전부 삭제한다

7. 최대한 깔끔하게 핵심기능만 있는 웹 나비스 만드는게 현재에 최종목표다.

\*/
