# navis 작업 완료 알림 (MCP)

실질적인 작업(코드 변경·버그 수정·리팩터링·빌드·배포 등)을 완료하면, 마지막에
`mcp__navis-notify__notify` 도구를 호출해 navis 채팅방으로 완료 보고를 보낸다.
→ 사용자가 맥/데스크톱에서 네이티브 알림으로 받는다.

- `text` 에 무엇을/왜 했는지 한두 문장으로 요약하고, 가능하면 커밋 해시를 포함한다.
- 한 번의 사용자 요청에 대해 작업이 모두 끝난 시점에 1회만 보낸다.
- 단순 질문 답변·사소한 한 줄 수정·탐색만 한 턴은 보내지 않는다(namory 자동저장과 동일 기준).
- 이 MCP 가 연결되어 있지 않으면(도구가 안 보이면) 조용히 생략한다.

> 도구가 없을 때의 수동 대체: `node scripts/notify-navis.mjs "<요약>"`
