packages:
  - "apps/*"
  - "packages/*"

# 네이티브 빌드 스크립트 허용 목록 — pnpm 은 기본적으로 전부 막는다.
#   esbuild        : tsx(서버 dev watch) 와 eslint 가 쓴다
#   unrs-resolver  : eslint-config-next 의 resolver
allowBuilds:
  esbuild: true
  unrs-resolver: true
